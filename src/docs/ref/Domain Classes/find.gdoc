h1. find

h2. Purpose

Executes an OQL query and returns a single result.

h2. Examples

{code:java}
      def result = Plant.find("name = 'rose'")
{code}

{code}
      def result = Plant.find('name = $1', ['rose'])
{code}



