h1. findAll

h2. Purpose

Executes an OQL query and returns the results.

h2. Examples

{code:java}
def results = Plant.findAll("goesInPatch = false")
{code}

{code}
def results = Plant.findAll('goesInPatch = $1', [false])
{code}

{code}
def results = Plant.findAll("goesInPatch = false", [sort:'name', order:"desc"])
{code}


{code}
def results = Plant.findAll('goesInPatch = $1', [false], [sort:'name'])
{code}




